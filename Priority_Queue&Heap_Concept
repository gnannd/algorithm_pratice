priority queue: 입력되는 순서에 따라 자료를 저장하고 선입 선출이 되는 queue와는 다르게 자료를 출력할 때, 자료에 부여된 우선순위 따라 높은것이 먼저 출력되도록 하는 자료구조.
삽입, 출력 둘다 O(lgn)시간이 걸림.
구현 : Heap으로 간단하게 구현. -> 표준 라이브러리에 이미 구현되어 있음.
보통 min-heap, max - heap이라고 최대 원소, 최소 원소 값을 root로 하는 이진 트리로 최대원소값을 꺼내는 연산을 lgn 만에 할 수 있게함.
그게 꼭 원소값을 기준으로 안하고 특정 값을 기준으로 만들어 응용함.

Heap: 특정한 규칙을 만족하도록 구성된 이진 트리
-부모와 자식 관계에 대해서만 규칙이 적용됌. -> 자식 끼리는 X
-Heap은 이진 트리 구조를 유지 하기 위해 다음과 같은 조건으로 이루어짐 -> heap의 높이는 O(lgN)이라는 것을 항상 만족하게 됌.
--1.마지막 레벨을 제외한 모든 레벨에 노드가 꽉 차있어야함.
--2.마지막 레벨에 노드가 있을 때는 항상 가장 왼쪽부터 순서대로 채워져 있어야함.
->결국 이러한 모양을 유지하기 때문에 배열로 표현이 가능함.

Haep 삽입 과정: 모양을 맞추기 위해 배열 맨끝에 원소 추가를 먼저한다음 부모노드와 비교해서 대소관계 규칙을 만족할 때까지 계속 부모노드랑 위치를 스왑해나가면 됌. 시간복잡도는 트리의 깊이에 비례 O(lgn)
Heap 삭제 과정: 루트를 꺼내기 때문에, 마지막 원소 를 루트로 대체하여 모양을 먼저 맞춘 후, 대소관계 규칙이 만족할 때까지 대체한 값을 자식들이랑 비교하며 스왑해 나가면됌, 이것도 또한 트리의 깊이에 비례하여 시간이 걸림 O(lgn)


