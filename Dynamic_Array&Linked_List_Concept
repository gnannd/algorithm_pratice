동적배열: 자료의 개수가 변함에 따라 크기 변경되는 배열(자료구조)
-구현:정적 배열에 다음과 특성이 추가됌.
  resize: 배열의 크기를 변경하는 연산 ->그냥 새로운 크기의 배열하나 만들어서 다복사하고 포인터로 다시 지정하는거. 수행시간 크기에 비례
  append: 맨끝에 원소 하나 추가 -> 메모리 할당받을 때 여유분으로 좀더 할당받아 원소가 추가될 때 수행시간 상수시간에 비례
  다만, 할당받은 용량을 다찼으면 resize를 통해 메모리 재할당받아야함.
--append 연산이 상수시간에 비레하게 할려면: 메모리가 다차 resize 재할당할 때, 현재 배열크기의 두배를 재할당받으면 됌. 증명은 간단해서 생략.
    이렇듯 재할당전략을 잘짜야한다.
    최종적으로 동적배열은 배열과 거의 다를게 없는 속도를 제공함. 
  참고로, c++경우 구현되어 있어(ex. vector) 직접 구현하지 않아도 됌.


연결리스트: 각 노드가 데이터와 다음 노드를 가리키는 포인터를 가지고, 포인터를 통해 순차적으로 연결된 자료구조
  배열이 연속된메모리 공간에 원소들이 저장되어 있는거면, 연결리스트는 각 원소들이 메모리 여기저기 흩어져있고 각 포인터로 다음 원소를 가르키는 방식으로 구현되어 있음.
  장점:특정위치의 삽입과 삭제를 상수시간에 할 수 있게 해줌.
  단점:탐색하는데 리스트 길이에 비례하는 선형시간이 걸림.
  참고로 이거 또한, c++경우(ex. list) 구현되어 있어 직접 구현하지 않아도 됌.

임의 원소접근을 주로 할 때는 동적배열을(연속된 메모리 할당이라 캐시효율도 좋음), 모든 원소들을 탐색하면서
삽입과 삭제가 필요할 때(문제를 풀다 보면 모든 원소들을 순회해서 조건에 맞는 위치에 삭제 또는 삽입 작업해야하는 경우가 많음)는 연결리스트를 사용하는 것이 좋음. 
