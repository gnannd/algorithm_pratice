---string 객체 관련.

string &ret= memo[x]; //이렇게 &을 쓰면 참조하되 *같은 역참조 안써도 됌. 즉 memo==ret 같은 객체가 된다 같은 공간을 공유하게 됌.
string은 c++에서 문자열 객체이다. vector와 같은.
객체이름 자체가 주소가 아님. 배열이름 자체가 첫번째 원소의 주소를 의미하는 것과 다르게.
객체의 주소를 표시하고 싶으면 &을 앞에 붙이면 됌. &memo[x]
